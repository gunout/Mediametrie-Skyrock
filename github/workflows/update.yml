name: Mise à jour hebdomadaire Skyrock

on:
  schedule:
    # Exécution tous les lundis à 8h00
    - cron: '0 8 * * 1'
  workflow_dispatch:  # Permet l'exécution manuelle

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout du dépôt
        uses: actions/checkout@v3

      - name: 🐍 Configuration Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 📦 Installation des dépendances
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      - name: 🔄 Exécution du scraper
        run: python scraper.py
        continue-on-error: true  # Ne pas bloquer si le scraping échoue

      - name: ✅ Vérification des modifications
        id: verify
        run: |
          if git diff --quiet data.json; then
            echo "Aucune modification détectée"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "Modifications détectées"
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: 📝 Commit des modifications
        if: steps.verify.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data.json
          git commit -m "📊 Mise à jour hebdomadaire des données - $(date +'%d/%m/%Y')"
          git push

      - name: 📧 Notification (optionnel)
        if: failure()
        run: |
          echo "⚠️ La mise à jour hebdomadaire a échoué"
          # Vous pouvez ajouter ici une notification par email ou Discord